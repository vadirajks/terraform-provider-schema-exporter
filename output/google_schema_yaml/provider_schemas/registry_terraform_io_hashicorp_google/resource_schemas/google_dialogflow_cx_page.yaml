version: 0
block:
  attributes:
    display_name:
      type: string
      description: The human-readable name of the page, unique within the agent.
      description_kind: plain
      required: true
    id:
      type: string
      description_kind: plain
      optional: true
      computed: true
    language_code:
      type: string
      description: 'The language of the following fields in page: Page.entry_fulfillment.messages Page.entry_fulfillment.conditional_cases Page.event_handlers.trigger_fulfillment.messages Page.event_handlers.trigger_fulfillment.conditional_cases Page.form.parameters.fill_behavior.initial_prompt_fulfillment.messages Page.form.parameters.fill_behavior.initial_prompt_fulfillment.conditional_cases Page.form.parameters.fill_behavior.reprompt_event_handlers.messages Page.form.parameters.fill_behavior.reprompt_event_handlers.conditional_cases Page.transition_routes.trigger_fulfillment.messages Page.transition_routes.trigger_fulfillment.conditional_cases If not specified, the agent''s default language is used. Many languages are supported. Note: languages must be enabled in the agent before they can be used.'
      description_kind: plain
      optional: true
    name:
      type: string
      description: 'The unique identifier of the page. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/flows/<Flow ID>/pages/<Page ID>.'
      description_kind: plain
      computed: true
    parent:
      type: string
      description: 'The flow to create a page for. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/flows/<Flow ID>.'
      description_kind: plain
      optional: true
    transition_route_groups:
      type:
      - list
      - string
      description: 'Ordered list of TransitionRouteGroups associated with the page. Transition route groups must be unique within a page. If multiple transition routes within a page scope refer to the same intent, then the precedence order is: page''s transition route -> page''s transition route group -> flow''s transition routes. If multiple transition route groups within a page contain the same intent, then the first group in the ordered list takes precedence. Format:projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/flows/<Flow ID>/transitionRouteGroups/<TransitionRouteGroup ID>.'
      description_kind: plain
      optional: true
  block_types:
    advanced_settings:
      nesting_mode: list
      block:
        block_types:
          dtmf_settings:
            nesting_mode: list
            block:
              attributes:
                enabled:
                  type: bool
                  description: If true, incoming audio is processed for DTMF (dual tone multi frequency) events. For example, if the caller presses a button on their telephone keypad and DTMF processing is enabled, Dialogflow will detect the event (e.g. a "3" was pressed) in the incoming audio and pass the event to the bot to drive business logic (e.g. when 3 is pressed, return the account balance).
                  description_kind: plain
                  optional: true
                finish_digit:
                  type: string
                  description: The digit that terminates a DTMF digit sequence.
                  description_kind: plain
                  optional: true
                max_digits:
                  type: number
                  description: Max length of DTMF digits.
                  description_kind: plain
                  optional: true
              description: 'Define behaviors for DTMF (dual tone multi frequency). DTMF settings does not override each other. DTMF settings set at different levels define DTMF detections running in parallel. Exposed at the following levels: * Agent level * Flow level * Page level * Parameter level'
              description_kind: plain
            max_items: 1
        description: 'Hierarchical advanced settings for this page. The settings exposed at the lower level overrides the settings exposed at the higher level. Hierarchy: Agent->Flow->Page->Fulfillment/Parameter.'
        description_kind: plain
      max_items: 1
    entry_fulfillment:
      nesting_mode: list
      block:
        attributes:
          return_partial_responses:
            type: bool
            description: 'Whether Dialogflow should return currently queued fulfillment response messages in streaming APIs. If a webhook is specified, it happens before Dialogflow invokes webhook. Warning: 1) This flag only affects streaming API. Responses are still queued and returned once in non-streaming API. 2) The flag can be enabled in any fulfillment but only the first 3 partial responses will be returned. You may only want to apply it to fulfillments that have slow webhooks.'
            description_kind: plain
            optional: true
          tag:
            type: string
            description: The tag used by the webhook to identify which fulfillment is being called. This field is required if webhook is specified.
            description_kind: plain
            optional: true
          webhook:
            type: string
            description: 'The webhook to call. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/webhooks/<Webhook ID>.'
            description_kind: plain
            optional: true
        block_types:
          conditional_cases:
            nesting_mode: list
            block:
              attributes:
                cases:
                  type: string
                  description: A JSON encoded list of cascading if-else conditions. Cases are mutually exclusive. The first one with a matching condition is selected, all the rest ignored. See [Case](https://cloud.google.com/dialogflow/cx/docs/reference/rest/v3/Fulfillment#case) for the schema.
                  description_kind: plain
                  optional: true
              description: Conditional cases for this fulfillment.
              description_kind: plain
          messages:
            nesting_mode: list
            block:
              attributes:
                channel:
                  type: string
                  description: The channel which the response is associated with. Clients can specify the channel via QueryParameters.channel, and only associated channel response will be returned.
                  description_kind: plain
                  optional: true
                payload:
                  type: string
                  description: A custom, platform-specific payload.
                  description_kind: plain
                  optional: true
              block_types:
                conversation_success:
                  nesting_mode: list
                  block:
                    attributes:
                      metadata:
                        type: string
                        description: Custom metadata. Dialogflow doesn't impose any structure on this.
                        description_kind: plain
                        optional: true
                    description: 'Indicates that the conversation succeeded, i.e., the bot handled the issue that the customer talked to it about. Dialogflow only uses this to determine which conversations should be counted as successful and doesn''t process the metadata in this message in any way. Note that Dialogflow also considers conversations that get to the conversation end page as successful even if they don''t return ConversationSuccess. You may set this, for example: * In the entryFulfillment of a Page if entering the page indicates that the conversation succeeded. * In a webhook response when you determine that you handled the customer issue.'
                    description_kind: plain
                  max_items: 1
                live_agent_handoff:
                  nesting_mode: list
                  block:
                    attributes:
                      metadata:
                        type: string
                        description: Custom metadata. Dialogflow doesn't impose any structure on this.
                        description_kind: plain
                        optional: true
                    description: 'Indicates that the conversation should be handed off to a live agent. Dialogflow only uses this to determine which conversations were handed off to a human agent for measurement purposes. What else to do with this signal is up to you and your handoff procedures. You may set this, for example: * In the entryFulfillment of a Page if entering the page indicates something went extremely wrong in the conversation. * In a webhook response when you determine that the customer issue can only be handled by a human.'
                    description_kind: plain
                  max_items: 1
                output_audio_text:
                  nesting_mode: list
                  block:
                    attributes:
                      allow_playback_interruption:
                        type: bool
                        description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                        description_kind: plain
                        computed: true
                      ssml:
                        type: string
                        description: The SSML text to be synthesized. For more information, see SSML.
                        description_kind: plain
                        optional: true
                      text:
                        type: string
                        description: The raw text to be synthesized.
                        description_kind: plain
                        optional: true
                    description: A text or ssml response that is preferentially used for TTS output audio synthesis, as described in the comment on the ResponseMessage message.
                    description_kind: plain
                  max_items: 1
                play_audio:
                  nesting_mode: list
                  block:
                    attributes:
                      allow_playback_interruption:
                        type: bool
                        description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                        description_kind: plain
                        computed: true
                      audio_uri:
                        type: string
                        description: URI of the audio clip. Dialogflow does not impose any validation on this value. It is specific to the client that reads it.
                        description_kind: plain
                        required: true
                    description: Specifies an audio clip to be played by the client as part of the response.
                    description_kind: plain
                  max_items: 1
                telephony_transfer_call:
                  nesting_mode: list
                  block:
                    attributes:
                      phone_number:
                        type: string
                        description: Transfer the call to a phone number in E.164 format.
                        description_kind: plain
                        required: true
                    description: Represents the signal that telles the client to transfer the phone call connected to the agent to a third-party endpoint.
                    description_kind: plain
                  max_items: 1
                text:
                  nesting_mode: list
                  block:
                    attributes:
                      allow_playback_interruption:
                        type: bool
                        description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                        description_kind: plain
                        computed: true
                      text:
                        type:
                        - list
                        - string
                        description: A collection of text responses.
                        description_kind: plain
                        optional: true
                    description: The text response message.
                    description_kind: plain
                  max_items: 1
              description: The list of rich message responses to present to the user.
              description_kind: plain
          set_parameter_actions:
            nesting_mode: list
            block:
              attributes:
                parameter:
                  type: string
                  description: Display name of the parameter.
                  description_kind: plain
                  optional: true
                value:
                  type: string
                  description: The new JSON-encoded value of the parameter. A null value clears the parameter.
                  description_kind: plain
                  optional: true
              description: Set parameter values before executing the webhook.
              description_kind: plain
        description: The fulfillment to call when the session is entering the page.
        description_kind: plain
      max_items: 1
    event_handlers:
      nesting_mode: list
      block:
        attributes:
          event:
            type: string
            description: The name of the event to handle.
            description_kind: plain
            optional: true
          name:
            type: string
            description: The unique identifier of this event handler.
            description_kind: plain
            computed: true
          target_flow:
            type: string
            description: 'The target flow to transition to. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/flows/<Flow ID>.'
            description_kind: plain
            optional: true
          target_page:
            type: string
            description: 'The target page to transition to. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/flows/<Flow ID>/pages/<Page ID>.'
            description_kind: plain
            optional: true
        block_types:
          trigger_fulfillment:
            nesting_mode: list
            block:
              attributes:
                return_partial_responses:
                  type: bool
                  description: 'Whether Dialogflow should return currently queued fulfillment response messages in streaming APIs. If a webhook is specified, it happens before Dialogflow invokes webhook. Warning: 1) This flag only affects streaming API. Responses are still queued and returned once in non-streaming API. 2) The flag can be enabled in any fulfillment but only the first 3 partial responses will be returned. You may only want to apply it to fulfillments that have slow webhooks.'
                  description_kind: plain
                  optional: true
                tag:
                  type: string
                  description: The tag used by the webhook to identify which fulfillment is being called. This field is required if webhook is specified.
                  description_kind: plain
                  optional: true
                webhook:
                  type: string
                  description: 'The webhook to call. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/webhooks/<Webhook ID>.'
                  description_kind: plain
                  optional: true
              block_types:
                conditional_cases:
                  nesting_mode: list
                  block:
                    attributes:
                      cases:
                        type: string
                        description: A JSON encoded list of cascading if-else conditions. Cases are mutually exclusive. The first one with a matching condition is selected, all the rest ignored. See [Case](https://cloud.google.com/dialogflow/cx/docs/reference/rest/v3/Fulfillment#case) for the schema.
                        description_kind: plain
                        optional: true
                    description: Conditional cases for this fulfillment.
                    description_kind: plain
                messages:
                  nesting_mode: list
                  block:
                    attributes:
                      channel:
                        type: string
                        description: The channel which the response is associated with. Clients can specify the channel via QueryParameters.channel, and only associated channel response will be returned.
                        description_kind: plain
                        optional: true
                      payload:
                        type: string
                        description: A custom, platform-specific payload.
                        description_kind: plain
                        optional: true
                    block_types:
                      conversation_success:
                        nesting_mode: list
                        block:
                          attributes:
                            metadata:
                              type: string
                              description: Custom metadata. Dialogflow doesn't impose any structure on this.
                              description_kind: plain
                              optional: true
                          description: 'Indicates that the conversation succeeded, i.e., the bot handled the issue that the customer talked to it about. Dialogflow only uses this to determine which conversations should be counted as successful and doesn''t process the metadata in this message in any way. Note that Dialogflow also considers conversations that get to the conversation end page as successful even if they don''t return ConversationSuccess. You may set this, for example: * In the entryFulfillment of a Page if entering the page indicates that the conversation succeeded. * In a webhook response when you determine that you handled the customer issue.'
                          description_kind: plain
                        max_items: 1
                      live_agent_handoff:
                        nesting_mode: list
                        block:
                          attributes:
                            metadata:
                              type: string
                              description: Custom metadata. Dialogflow doesn't impose any structure on this.
                              description_kind: plain
                              optional: true
                          description: 'Indicates that the conversation should be handed off to a live agent. Dialogflow only uses this to determine which conversations were handed off to a human agent for measurement purposes. What else to do with this signal is up to you and your handoff procedures. You may set this, for example: * In the entryFulfillment of a Page if entering the page indicates something went extremely wrong in the conversation. * In a webhook response when you determine that the customer issue can only be handled by a human.'
                          description_kind: plain
                        max_items: 1
                      output_audio_text:
                        nesting_mode: list
                        block:
                          attributes:
                            allow_playback_interruption:
                              type: bool
                              description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                              description_kind: plain
                              computed: true
                            ssml:
                              type: string
                              description: The SSML text to be synthesized. For more information, see SSML.
                              description_kind: plain
                              optional: true
                            text:
                              type: string
                              description: The raw text to be synthesized.
                              description_kind: plain
                              optional: true
                          description: A text or ssml response that is preferentially used for TTS output audio synthesis, as described in the comment on the ResponseMessage message.
                          description_kind: plain
                        max_items: 1
                      play_audio:
                        nesting_mode: list
                        block:
                          attributes:
                            allow_playback_interruption:
                              type: bool
                              description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                              description_kind: plain
                              computed: true
                            audio_uri:
                              type: string
                              description: URI of the audio clip. Dialogflow does not impose any validation on this value. It is specific to the client that reads it.
                              description_kind: plain
                              required: true
                          description: Specifies an audio clip to be played by the client as part of the response.
                          description_kind: plain
                        max_items: 1
                      telephony_transfer_call:
                        nesting_mode: list
                        block:
                          attributes:
                            phone_number:
                              type: string
                              description: Transfer the call to a phone number in E.164 format.
                              description_kind: plain
                              required: true
                          description: Represents the signal that telles the client to transfer the phone call connected to the agent to a third-party endpoint.
                          description_kind: plain
                        max_items: 1
                      text:
                        nesting_mode: list
                        block:
                          attributes:
                            allow_playback_interruption:
                              type: bool
                              description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                              description_kind: plain
                              computed: true
                            text:
                              type:
                              - list
                              - string
                              description: A collection of text responses.
                              description_kind: plain
                              optional: true
                          description: The text response message.
                          description_kind: plain
                        max_items: 1
                    description: The list of rich message responses to present to the user.
                    description_kind: plain
                set_parameter_actions:
                  nesting_mode: list
                  block:
                    attributes:
                      parameter:
                        type: string
                        description: Display name of the parameter.
                        description_kind: plain
                        optional: true
                      value:
                        type: string
                        description: The new JSON-encoded value of the parameter. A null value clears the parameter.
                        description_kind: plain
                        optional: true
                    description: Set parameter values before executing the webhook.
                    description_kind: plain
              description: The fulfillment to call when the event occurs. Handling webhook errors with a fulfillment enabled with webhook could cause infinite loop. It is invalid to specify such fulfillment for a handler handling webhooks.
              description_kind: plain
            max_items: 1
        description: Handlers associated with the page to handle events such as webhook errors, no match or no input.
        description_kind: plain
    form:
      nesting_mode: list
      block:
        block_types:
          parameters:
            nesting_mode: list
            block:
              attributes:
                default_value:
                  type: string
                  description: The default value of an optional parameter. If the parameter is required, the default value will be ignored.
                  description_kind: plain
                  optional: true
                display_name:
                  type: string
                  description: The human-readable name of the parameter, unique within the form.
                  description_kind: plain
                  optional: true
                entity_type:
                  type: string
                  description: 'The entity type of the parameter. Format: projects/-/locations/-/agents/-/entityTypes/<System Entity Type ID> for system entity types (for example, projects/-/locations/-/agents/-/entityTypes/sys.date), or projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/entityTypes/<Entity Type ID> for developer entity types.'
                  description_kind: plain
                  optional: true
                is_list:
                  type: bool
                  description: Indicates whether the parameter represents a list of values.
                  description_kind: plain
                  optional: true
                redact:
                  type: bool
                  description: 'Indicates whether the parameter content should be redacted in log. If redaction is enabled, the parameter content will be replaced by parameter name during logging. Note: the parameter content is subject to redaction if either parameter level redaction or entity type level redaction is enabled.'
                  description_kind: plain
                  optional: true
                required:
                  type: bool
                  description: Indicates whether the parameter is required. Optional parameters will not trigger prompts; however, they are filled if the user specifies them. Required parameters must be filled before form filling concludes.
                  description_kind: plain
                  optional: true
              block_types:
                advanced_settings:
                  nesting_mode: list
                  block:
                    block_types:
                      dtmf_settings:
                        nesting_mode: list
                        block:
                          attributes:
                            enabled:
                              type: bool
                              description: If true, incoming audio is processed for DTMF (dual tone multi frequency) events. For example, if the caller presses a button on their telephone keypad and DTMF processing is enabled, Dialogflow will detect the event (e.g. a "3" was pressed) in the incoming audio and pass the event to the bot to drive business logic (e.g. when 3 is pressed, return the account balance).
                              description_kind: plain
                              optional: true
                            finish_digit:
                              type: string
                              description: The digit that terminates a DTMF digit sequence.
                              description_kind: plain
                              optional: true
                            max_digits:
                              type: number
                              description: Max length of DTMF digits.
                              description_kind: plain
                              optional: true
                          description: 'Define behaviors for DTMF (dual tone multi frequency). DTMF settings does not override each other. DTMF settings set at different levels define DTMF detections running in parallel. Exposed at the following levels: * Agent level * Flow level * Page level * Parameter level'
                          description_kind: plain
                        max_items: 1
                    description: 'Hierarchical advanced settings for this parameter. The settings exposed at the lower level overrides the settings exposed at the higher level. Hierarchy: Agent->Flow->Page->Fulfillment/Parameter.'
                    description_kind: plain
                  max_items: 1
                fill_behavior:
                  nesting_mode: list
                  block:
                    block_types:
                      initial_prompt_fulfillment:
                        nesting_mode: list
                        block:
                          attributes:
                            return_partial_responses:
                              type: bool
                              description: 'Whether Dialogflow should return currently queued fulfillment response messages in streaming APIs. If a webhook is specified, it happens before Dialogflow invokes webhook. Warning: 1) This flag only affects streaming API. Responses are still queued and returned once in non-streaming API. 2) The flag can be enabled in any fulfillment but only the first 3 partial responses will be returned. You may only want to apply it to fulfillments that have slow webhooks.'
                              description_kind: plain
                              optional: true
                            tag:
                              type: string
                              description: The tag used by the webhook to identify which fulfillment is being called. This field is required if webhook is specified.
                              description_kind: plain
                              optional: true
                            webhook:
                              type: string
                              description: 'The webhook to call. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/webhooks/<Webhook ID>.'
                              description_kind: plain
                              optional: true
                          block_types:
                            conditional_cases:
                              nesting_mode: list
                              block:
                                attributes:
                                  cases:
                                    type: string
                                    description: A JSON encoded list of cascading if-else conditions. Cases are mutually exclusive. The first one with a matching condition is selected, all the rest ignored. See [Case](https://cloud.google.com/dialogflow/cx/docs/reference/rest/v3/Fulfillment#case) for the schema.
                                    description_kind: plain
                                    optional: true
                                description: Conditional cases for this fulfillment.
                                description_kind: plain
                            messages:
                              nesting_mode: list
                              block:
                                attributes:
                                  channel:
                                    type: string
                                    description: The channel which the response is associated with. Clients can specify the channel via QueryParameters.channel, and only associated channel response will be returned.
                                    description_kind: plain
                                    optional: true
                                  payload:
                                    type: string
                                    description: A custom, platform-specific payload.
                                    description_kind: plain
                                    optional: true
                                block_types:
                                  conversation_success:
                                    nesting_mode: list
                                    block:
                                      attributes:
                                        metadata:
                                          type: string
                                          description: Custom metadata. Dialogflow doesn't impose any structure on this.
                                          description_kind: plain
                                          optional: true
                                      description: 'Indicates that the conversation succeeded, i.e., the bot handled the issue that the customer talked to it about. Dialogflow only uses this to determine which conversations should be counted as successful and doesn''t process the metadata in this message in any way. Note that Dialogflow also considers conversations that get to the conversation end page as successful even if they don''t return ConversationSuccess. You may set this, for example: * In the entryFulfillment of a Page if entering the page indicates that the conversation succeeded. * In a webhook response when you determine that you handled the customer issue.'
                                      description_kind: plain
                                    max_items: 1
                                  live_agent_handoff:
                                    nesting_mode: list
                                    block:
                                      attributes:
                                        metadata:
                                          type: string
                                          description: Custom metadata. Dialogflow doesn't impose any structure on this.
                                          description_kind: plain
                                          optional: true
                                      description: 'Indicates that the conversation should be handed off to a live agent. Dialogflow only uses this to determine which conversations were handed off to a human agent for measurement purposes. What else to do with this signal is up to you and your handoff procedures. You may set this, for example: * In the entryFulfillment of a Page if entering the page indicates something went extremely wrong in the conversation. * In a webhook response when you determine that the customer issue can only be handled by a human.'
                                      description_kind: plain
                                    max_items: 1
                                  output_audio_text:
                                    nesting_mode: list
                                    block:
                                      attributes:
                                        allow_playback_interruption:
                                          type: bool
                                          description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                                          description_kind: plain
                                          computed: true
                                        ssml:
                                          type: string
                                          description: The SSML text to be synthesized. For more information, see SSML.
                                          description_kind: plain
                                          optional: true
                                        text:
                                          type: string
                                          description: The raw text to be synthesized.
                                          description_kind: plain
                                          optional: true
                                      description: A text or ssml response that is preferentially used for TTS output audio synthesis, as described in the comment on the ResponseMessage message.
                                      description_kind: plain
                                    max_items: 1
                                  play_audio:
                                    nesting_mode: list
                                    block:
                                      attributes:
                                        allow_playback_interruption:
                                          type: bool
                                          description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                                          description_kind: plain
                                          computed: true
                                        audio_uri:
                                          type: string
                                          description: URI of the audio clip. Dialogflow does not impose any validation on this value. It is specific to the client that reads it.
                                          description_kind: plain
                                          required: true
                                      description: Specifies an audio clip to be played by the client as part of the response.
                                      description_kind: plain
                                    max_items: 1
                                  telephony_transfer_call:
                                    nesting_mode: list
                                    block:
                                      attributes:
                                        phone_number:
                                          type: string
                                          description: Transfer the call to a phone number in E.164 format.
                                          description_kind: plain
                                          required: true
                                      description: Represents the signal that telles the client to transfer the phone call connected to the agent to a third-party endpoint.
                                      description_kind: plain
                                    max_items: 1
                                  text:
                                    nesting_mode: list
                                    block:
                                      attributes:
                                        allow_playback_interruption:
                                          type: bool
                                          description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                                          description_kind: plain
                                          computed: true
                                        text:
                                          type:
                                          - list
                                          - string
                                          description: A collection of text responses.
                                          description_kind: plain
                                          optional: true
                                      description: The text response message.
                                      description_kind: plain
                                    max_items: 1
                                description: The list of rich message responses to present to the user.
                                description_kind: plain
                            set_parameter_actions:
                              nesting_mode: list
                              block:
                                attributes:
                                  parameter:
                                    type: string
                                    description: Display name of the parameter.
                                    description_kind: plain
                                    optional: true
                                  value:
                                    type: string
                                    description: The new JSON-encoded value of the parameter. A null value clears the parameter.
                                    description_kind: plain
                                    optional: true
                                description: Set parameter values before executing the webhook.
                                description_kind: plain
                          description: The fulfillment to provide the initial prompt that the agent can present to the user in order to fill the parameter.
                          description_kind: plain
                        max_items: 1
                      reprompt_event_handlers:
                        nesting_mode: list
                        block:
                          attributes:
                            event:
                              type: string
                              description: The name of the event to handle.
                              description_kind: plain
                              optional: true
                            name:
                              type: string
                              description: The unique identifier of this event handler.
                              description_kind: plain
                              computed: true
                            target_flow:
                              type: string
                              description: 'The target flow to transition to. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/flows/<Flow ID>.'
                              description_kind: plain
                              optional: true
                            target_page:
                              type: string
                              description: 'The target page to transition to. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/flows/<Flow ID>/pages/<Page ID>.'
                              description_kind: plain
                              optional: true
                          block_types:
                            trigger_fulfillment:
                              nesting_mode: list
                              block:
                                attributes:
                                  return_partial_responses:
                                    type: bool
                                    description: 'Whether Dialogflow should return currently queued fulfillment response messages in streaming APIs. If a webhook is specified, it happens before Dialogflow invokes webhook. Warning: 1) This flag only affects streaming API. Responses are still queued and returned once in non-streaming API. 2) The flag can be enabled in any fulfillment but only the first 3 partial responses will be returned. You may only want to apply it to fulfillments that have slow webhooks.'
                                    description_kind: plain
                                    optional: true
                                  tag:
                                    type: string
                                    description: The tag used by the webhook to identify which fulfillment is being called. This field is required if webhook is specified.
                                    description_kind: plain
                                    optional: true
                                  webhook:
                                    type: string
                                    description: 'The webhook to call. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/webhooks/<Webhook ID>.'
                                    description_kind: plain
                                    optional: true
                                block_types:
                                  conditional_cases:
                                    nesting_mode: list
                                    block:
                                      attributes:
                                        cases:
                                          type: string
                                          description: A JSON encoded list of cascading if-else conditions. Cases are mutually exclusive. The first one with a matching condition is selected, all the rest ignored. See [Case](https://cloud.google.com/dialogflow/cx/docs/reference/rest/v3/Fulfillment#case) for the schema.
                                          description_kind: plain
                                          optional: true
                                      description: Conditional cases for this fulfillment.
                                      description_kind: plain
                                  messages:
                                    nesting_mode: list
                                    block:
                                      attributes:
                                        channel:
                                          type: string
                                          description: The channel which the response is associated with. Clients can specify the channel via QueryParameters.channel, and only associated channel response will be returned.
                                          description_kind: plain
                                          optional: true
                                        payload:
                                          type: string
                                          description: A custom, platform-specific payload.
                                          description_kind: plain
                                          optional: true
                                      block_types:
                                        conversation_success:
                                          nesting_mode: list
                                          block:
                                            attributes:
                                              metadata:
                                                type: string
                                                description: Custom metadata. Dialogflow doesn't impose any structure on this.
                                                description_kind: plain
                                                optional: true
                                            description: 'Indicates that the conversation succeeded, i.e., the bot handled the issue that the customer talked to it about. Dialogflow only uses this to determine which conversations should be counted as successful and doesn''t process the metadata in this message in any way. Note that Dialogflow also considers conversations that get to the conversation end page as successful even if they don''t return ConversationSuccess. You may set this, for example: * In the entryFulfillment of a Page if entering the page indicates that the conversation succeeded. * In a webhook response when you determine that you handled the customer issue.'
                                            description_kind: plain
                                          max_items: 1
                                        live_agent_handoff:
                                          nesting_mode: list
                                          block:
                                            attributes:
                                              metadata:
                                                type: string
                                                description: Custom metadata. Dialogflow doesn't impose any structure on this.
                                                description_kind: plain
                                                optional: true
                                            description: 'Indicates that the conversation should be handed off to a live agent. Dialogflow only uses this to determine which conversations were handed off to a human agent for measurement purposes. What else to do with this signal is up to you and your handoff procedures. You may set this, for example: * In the entryFulfillment of a Page if entering the page indicates something went extremely wrong in the conversation. * In a webhook response when you determine that the customer issue can only be handled by a human.'
                                            description_kind: plain
                                          max_items: 1
                                        output_audio_text:
                                          nesting_mode: list
                                          block:
                                            attributes:
                                              allow_playback_interruption:
                                                type: bool
                                                description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                                                description_kind: plain
                                                computed: true
                                              ssml:
                                                type: string
                                                description: The SSML text to be synthesized. For more information, see SSML.
                                                description_kind: plain
                                                optional: true
                                              text:
                                                type: string
                                                description: The raw text to be synthesized.
                                                description_kind: plain
                                                optional: true
                                            description: A text or ssml response that is preferentially used for TTS output audio synthesis, as described in the comment on the ResponseMessage message.
                                            description_kind: plain
                                          max_items: 1
                                        play_audio:
                                          nesting_mode: list
                                          block:
                                            attributes:
                                              allow_playback_interruption:
                                                type: bool
                                                description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                                                description_kind: plain
                                                computed: true
                                              audio_uri:
                                                type: string
                                                description: URI of the audio clip. Dialogflow does not impose any validation on this value. It is specific to the client that reads it.
                                                description_kind: plain
                                                required: true
                                            description: Specifies an audio clip to be played by the client as part of the response.
                                            description_kind: plain
                                          max_items: 1
                                        telephony_transfer_call:
                                          nesting_mode: list
                                          block:
                                            attributes:
                                              phone_number:
                                                type: string
                                                description: Transfer the call to a phone number in E.164 format.
                                                description_kind: plain
                                                required: true
                                            description: Represents the signal that telles the client to transfer the phone call connected to the agent to a third-party endpoint.
                                            description_kind: plain
                                          max_items: 1
                                        text:
                                          nesting_mode: list
                                          block:
                                            attributes:
                                              allow_playback_interruption:
                                                type: bool
                                                description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                                                description_kind: plain
                                                computed: true
                                              text:
                                                type:
                                                - list
                                                - string
                                                description: A collection of text responses.
                                                description_kind: plain
                                                optional: true
                                            description: The text response message.
                                            description_kind: plain
                                          max_items: 1
                                      description: The list of rich message responses to present to the user.
                                      description_kind: plain
                                  set_parameter_actions:
                                    nesting_mode: list
                                    block:
                                      attributes:
                                        parameter:
                                          type: string
                                          description: Display name of the parameter.
                                          description_kind: plain
                                          optional: true
                                        value:
                                          type: string
                                          description: The new JSON-encoded value of the parameter. A null value clears the parameter.
                                          description_kind: plain
                                          optional: true
                                      description: Set parameter values before executing the webhook.
                                      description_kind: plain
                                description: The fulfillment to call when the event occurs. Handling webhook errors with a fulfillment enabled with webhook could cause infinite loop. It is invalid to specify such fulfillment for a handler handling webhooks.
                                description_kind: plain
                              max_items: 1
                          description: 'The handlers for parameter-level events, used to provide reprompt for the parameter or transition to a different page/flow. The supported events are: * sys.no-match-<N>, where N can be from 1 to 6 * sys.no-match-default * sys.no-input-<N>, where N can be from 1 to 6 * sys.no-input-default * sys.invalid-parameter [initialPromptFulfillment][initialPromptFulfillment] provides the first prompt for the parameter. If the user''s response does not fill the parameter, a no-match/no-input event will be triggered, and the fulfillment associated with the sys.no-match-1/sys.no-input-1 handler (if defined) will be called to provide a prompt. The sys.no-match-2/sys.no-input-2 handler (if defined) will respond to the next no-match/no-input event, and so on. A sys.no-match-default or sys.no-input-default handler will be used to handle all following no-match/no-input events after all numbered no-match/no-input handlers for the parameter are consumed. A sys.invalid-parameter handler can be defined to handle the case where the parameter values have been invalidated by webhook. For example, if the user''s response fill the parameter, however the parameter was invalidated by webhook, the fulfillment associated with the sys.invalid-parameter handler (if defined) will be called to provide a prompt. If the event handler for the corresponding event can''t be found on the parameter, initialPromptFulfillment will be re-prompted.'
                          description_kind: plain
                    description: Defines fill behavior for the parameter.
                    description_kind: plain
                  max_items: 1
              description: Parameters to collect from the user.
              description_kind: plain
        description: The form associated with the page, used for collecting parameters relevant to the page.
        description_kind: plain
      max_items: 1
    knowledge_connector_settings:
      nesting_mode: list
      block:
        attributes:
          enabled:
            type: bool
            description: Whether Knowledge Connector is enabled or not.
            description_kind: plain
            optional: true
          target_flow:
            type: string
            description: 'The target flow to transition to. Format: projects/<ProjectID>/locations/<LocationID>/agents/<AgentID>/flows/<FlowID>. This field is part of a union field ''target'': Only one of ''targetPage'' or ''targetFlow'' may be set.'
            description_kind: plain
            optional: true
          target_page:
            type: string
            description: 'The target page to transition to. Format: projects/<ProjectID>/locations/<LocationID>/agents/<AgentID>/flows/<FlowID>/pages/<PageID>. The page must be in the same host flow (the flow that owns this ''KnowledgeConnectorSettings''). This field is part of a union field ''target'': Only one of ''targetPage'' or ''targetFlow'' may be set.'
            description_kind: plain
            optional: true
        block_types:
          data_store_connections:
            nesting_mode: list
            block:
              attributes:
                data_store:
                  type: string
                  description: 'The full name of the referenced data store. Formats: projects/{project}/locations/{location}/collections/{collection}/dataStores/{dataStore} projects/{project}/locations/{location}/dataStores/{dataStore}'
                  description_kind: plain
                  optional: true
                data_store_type:
                  type: string
                  description: 'The type of the connected data store. * PUBLIC_WEB: A data store that contains public web content. * UNSTRUCTURED: A data store that contains unstructured private data. * STRUCTURED: A data store that contains structured data (for example FAQ). Possible values: ["PUBLIC_WEB", "UNSTRUCTURED", "STRUCTURED"]'
                  description_kind: plain
                  optional: true
                document_processing_mode:
                  type: string
                  description: 'The document processing mode for the data store connection. Should only be set for PUBLIC_WEB and UNSTRUCTURED data stores. If not set it is considered as DOCUMENTS, as this is the legacy mode. * DOCUMENTS: Documents are processed as documents. * CHUNKS: Documents are converted to chunks. Possible values: ["DOCUMENTS", "CHUNKS"]'
                  description_kind: plain
                  optional: true
              description: Optional. List of related data store connections.
              description_kind: plain
          trigger_fulfillment:
            nesting_mode: list
            block:
              attributes:
                enable_generative_fallback:
                  type: bool
                  description: If the flag is true, the agent will utilize LLM to generate a text response. If LLM generation fails, the defined responses in the fulfillment will be respected. This flag is only useful for fulfillments associated with no-match event handlers.
                  description_kind: plain
                  optional: true
                return_partial_responses:
                  type: bool
                  description: 'Whether Dialogflow should return currently queued fulfillment response messages in streaming APIs. If a webhook is specified, it happens before Dialogflow invokes webhook. Warning: 1) This flag only affects streaming API. Responses are still queued and returned once in non-streaming API. 2) The flag can be enabled in any fulfillment but only the first 3 partial responses will be returned. You may only want to apply it to fulfillments that have slow webhooks.'
                  description_kind: plain
                  optional: true
                tag:
                  type: string
                  description: The tag used by the webhook to identify which fulfillment is being called. This field is required if webhook is specified.
                  description_kind: plain
                  optional: true
                webhook:
                  type: string
                  description: 'The webhook to call. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/webhooks/<Webhook ID>.'
                  description_kind: plain
                  optional: true
              block_types:
                advanced_settings:
                  nesting_mode: list
                  block:
                    block_types:
                      dtmf_settings:
                        nesting_mode: list
                        block:
                          attributes:
                            enabled:
                              type: bool
                              description: If true, incoming audio is processed for DTMF (dual tone multi frequtectency) events. For example, if the caller presses a button on their telephone keypad and DTMF processing is enabled, Dialogflow will de the event (e.g. a "3" was pressed) in the incoming audio and pass the event to the bot to drive business logic (e.g. when 3 is pressed, return the account balance).
                              description_kind: plain
                              optional: true
                            endpointing_timeout_duration:
                              type: string
                              description: 'Endpoint timeout setting for matching dtmf input to regex. A duration in seconds with up to nine fractional digits, ending with ''s''. Example: "3.500s".'
                              description_kind: plain
                              optional: true
                            finish_digit:
                              type: string
                              description: The digit that terminates a DTMF digit sequence.
                              description_kind: plain
                              optional: true
                            interdigit_timeout_duration:
                              type: string
                              description: 'Interdigit timeout setting for matching dtmf input to regex. A duration in seconds with up to nine fractional digits, ending with ''s''. Example: "3.500s".'
                              description_kind: plain
                              optional: true
                            max_digits:
                              type: number
                              description: Max length of DTMF digits.
                              description_kind: plain
                              optional: true
                          description: 'Define behaviors for DTMF (dual tone multi frequency). DTMF settings does not override each other. DTMF settings set at different levels define DTMF detections running in parallel. Exposed at the following levels: * Agent level * Flow level * Page level * Parameter level'
                          description_kind: plain
                        max_items: 1
                      logging_settings:
                        nesting_mode: list
                        block:
                          attributes:
                            enable_consent_based_redaction:
                              type: bool
                              description: Enables consent-based end-user input redaction, if true, a pre-defined session parameter **$session.params.conversation-redaction** will be used to determine if the utterance should be redacted.
                              description_kind: plain
                              optional: true
                            enable_interaction_logging:
                              type: bool
                              description: Enables DF Interaction logging.
                              description_kind: plain
                              optional: true
                            enable_stackdriver_logging:
                              type: bool
                              description: Enables Google Cloud Logging.
                              description_kind: plain
                              optional: true
                          description: 'Settings for logging. Settings for Dialogflow History, Contact Center messages, StackDriver logs, and speech logging. Exposed at the following levels: * Agent level'
                          description_kind: plain
                        max_items: 1
                      speech_settings:
                        nesting_mode: list
                        block:
                          attributes:
                            endpointer_sensitivity:
                              type: number
                              description: Sensitivity of the speech model that detects the end of speech. Scale from 0 to 100.
                              description_kind: plain
                              optional: true
                            models:
                              type:
                              - map
                              - string
                              description: 'Mapping from language to Speech-to-Text model. The mapped Speech-to-Text model will be selected for requests from its corresponding language. For more information, see [Speech models](https://cloud.google.com/dialogflow/cx/docs/concept/speech-models). An object containing a list of **"key": value** pairs. Example: **{ "name": "wrench", "mass": "1.3kg", "count": "3" }**.'
                              description_kind: plain
                              optional: true
                            no_speech_timeout:
                              type: string
                              description: 'Timeout before detecting no speech. A duration in seconds with up to nine fractional digits, ending with ''s''. Example: "3.500s".'
                              description_kind: plain
                              optional: true
                            use_timeout_based_endpointing:
                              type: bool
                              description: Use timeout based endpointing, interpreting endpointer sensitivity as seconds of timeout value.
                              description_kind: plain
                              optional: true
                          description: 'Settings for speech to text detection. Exposed at the following levels: * Agent level * Flow level * Page level * Parameter level'
                          description_kind: plain
                        max_items: 1
                    description: 'Hierarchical advanced settings for agent/flow/page/fulfillment/parameter. Settings exposed at lower level overrides the settings exposed at higher level. Overriding occurs at the sub-setting level. For example, the playbackInterruptionSettings at fulfillment level only overrides the playbackInterruptionSettings at the agent level, leaving other settings at the agent level unchanged. DTMF settings does not override each other. DTMF settings set at different levels define DTMF detections running in parallel. Hierarchy: Agent->Flow->Page->Fulfillment/Parameter.'
                    description_kind: plain
                  max_items: 1
                conditional_cases:
                  nesting_mode: list
                  block:
                    attributes:
                      cases:
                        type: string
                        description: A JSON encoded list of cascading if-else conditions. Cases are mutually exclusive. The first one with a matching condition is selected, all the rest ignored. See [Case](https://cloud.google.com/dialogflow/cx/docs/reference/rest/v3/Fulfillment#case) for the schema.
                        description_kind: plain
                        optional: true
                    description: Conditional cases for this fulfillment.
                    description_kind: plain
                messages:
                  nesting_mode: list
                  block:
                    attributes:
                      channel:
                        type: string
                        description: The channel which the response is associated with. Clients can specify the channel via QueryParameters.channel, and only associated channel response will be returned.
                        description_kind: plain
                        optional: true
                      end_interaction:
                        type:
                        - list
                        - - object
                          - {}
                        description: 'This type has no fields. Indicates that interaction with the Dialogflow agent has ended. This message is generated by Dialogflow only and not supposed to be defined by the user. This field is part of a union field ''message'': Only one of ''text'', ''payload'', ''conversationSuccess'', ''outputAudioText'', ''liveAgentHandoff'', ''endInteraction'', ''playAudio'', ''mixedAudio'', ''telephonyTransferCall'', or ''knowledgeInfoCard'' may be set.'
                        description_kind: plain
                        computed: true
                      mixed_audio:
                        type:
                        - list
                        - - object
                          - segments:
                            - list
                            - - object
                              - allow_playback_interruption: bool
                                audio: string
                                uri: string
                        description: 'Represents an audio message that is composed of both segments synthesized from the Dialogflow agent prompts and ones hosted externally at the specified URIs. The external URIs are specified via playAudio. This message is generated by Dialogflow only and not supposed to be defined by the user. This field is part of a union field ''message'': Only one of ''text'', ''payload'', ''conversationSuccess'', ''outputAudioText'', ''liveAgentHandoff'', ''endInteraction'', ''playAudio'', ''mixedAudio'', ''telephonyTransferCall'', or ''knowledgeInfoCard'' may be set.'
                        description_kind: plain
                        computed: true
                      payload:
                        type: string
                        description: 'Returns a response containing a custom, platform-specific payload. This field is part of a union field ''message'': Only one of ''text'', ''payload'', ''conversationSuccess'', ''outputAudioText'', ''liveAgentHandoff'', ''endInteraction'', ''playAudio'', ''mixedAudio'', ''telephonyTransferCall'', or ''knowledgeInfoCard'' may be set.'
                        description_kind: plain
                        optional: true
                    block_types:
                      conversation_success:
                        nesting_mode: list
                        block:
                          attributes:
                            metadata:
                              type: string
                              description: Custom metadata. Dialogflow doesn't impose any structure on this.
                              description_kind: plain
                              optional: true
                          description: 'Indicates that the conversation succeeded, i.e., the bot handled the issue that the customer talked to it about. Dialogflow only uses this to determine which conversations should be counted as successful and doesn''t process the metadata in this message in any way. Note that Dialogflow also considers conversations that get to the conversation end page as successful even if they don''t return ConversationSuccess. You may set this, for example: * In the entryFulfillment of a Page if entering the page indicates that the conversation succeeded. * In a webhook response when you determine that you handled the customer issue. This field is part of a union field ''message'': Only one of ''text'', ''payload'', ''conversationSuccess'', ''outputAudioText'', ''liveAgentHandoff'', ''endInteraction'', ''playAudio'', ''mixedAudio'', ''telephonyTransferCall'', or ''knowledgeInfoCard'' may be set.'
                          description_kind: plain
                        max_items: 1
                      knowledge_info_card:
                        nesting_mode: list
                        block:
                          description: 'This type has no fields. Represents info card response. If the response contains generative knowledge prediction, Dialogflow will return a payload with Infobot Messenger compatible info card. Otherwise, the info card response is skipped. This field is part of a union field ''message'': Only one of ''text'', ''payload'', ''conversationSuccess'', ''outputAudioText'', ''liveAgentHandoff'', ''endInteraction'', ''playAudio'', ''mixedAudio'', ''telephonyTransferCall'', or ''knowledgeInfoCard'' may be set.'
                          description_kind: plain
                        max_items: 1
                      live_agent_handoff:
                        nesting_mode: list
                        block:
                          attributes:
                            metadata:
                              type: string
                              description: Custom metadata. Dialogflow doesn't impose any structure on this.
                              description_kind: plain
                              optional: true
                          description: 'Indicates that the conversation should be handed off to a live agent. Dialogflow only uses this to determine which conversations were handed off to a human agent for measurement purposes. What else to do with this signal is up to you and your handoff procedures. You may set this, for example: * In the entryFulfillment of a Page if entering the page indicates something went extremely wrong in the conversation. * In a webhook response when you determine that the customer issue can only be handled by a human. This field is part of a union field ''message'': Only one of ''text'', ''payload'', ''conversationSuccess'', ''outputAudioText'', ''liveAgentHandoff'', ''endInteraction'', ''playAudio'', ''mixedAudio'', ''telephonyTransferCall'', or ''knowledgeInfoCard'' may be set.'
                          description_kind: plain
                        max_items: 1
                      output_audio_text:
                        nesting_mode: list
                        block:
                          attributes:
                            allow_playback_interruption:
                              type: bool
                              description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                              description_kind: plain
                              computed: true
                            ssml:
                              type: string
                              description: 'The SSML text to be synthesized. For more information, see SSML. This field is part of a union field ''source'': Only one of ''text'' or ''ssml'' may be set.'
                              description_kind: plain
                              optional: true
                            text:
                              type: string
                              description: 'The raw text to be synthesized. This field is part of a union field ''source'': Only one of ''text'' or ''ssml'' may be set.'
                              description_kind: plain
                              optional: true
                          description: 'A text or ssml response that is preferentially used for TTS output audio synthesis, as described in the comment on the ResponseMessage message. This field is part of a union field ''message'': Only one of ''text'', ''payload'', ''conversationSuccess'', ''outputAudioText'', ''liveAgentHandoff'', ''endInteraction'', ''playAudio'', ''mixedAudio'', ''telephonyTransferCall'', or ''knowledgeInfoCard'' may be set.'
                          description_kind: plain
                        max_items: 1
                      play_audio:
                        nesting_mode: list
                        block:
                          attributes:
                            allow_playback_interruption:
                              type: bool
                              description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                              description_kind: plain
                              computed: true
                            audio_uri:
                              type: string
                              description: URI of the audio clip. Dialogflow does not impose any validation on this value. It is specific to the client that reads it.
                              description_kind: plain
                              required: true
                          description: 'Specifies an audio clip to be played by the client as part of the response. This field is part of a union field ''message'': Only one of ''text'', ''payload'', ''conversationSuccess'', ''outputAudioText'', ''liveAgentHandoff'', ''endInteraction'', ''playAudio'', ''mixedAudio'', ''telephonyTransferCall'', or ''knowledgeInfoCard'' may be set.'
                          description_kind: plain
                        max_items: 1
                      telephony_transfer_call:
                        nesting_mode: list
                        block:
                          attributes:
                            phone_number:
                              type: string
                              description: Transfer the call to a phone number in E.164 format.
                              description_kind: plain
                              required: true
                          description: 'Represents the signal that telles the client to transfer the phone call connected to the agent to a third-party endpoint. This field is part of a union field ''message'': Only one of ''text'', ''payload'', ''conversationSuccess'', ''outputAudioText'', ''liveAgentHandoff'', ''endInteraction'', ''playAudio'', ''mixedAudio'', ''telephonyTransferCall'', or ''knowledgeInfoCard'' may be set.'
                          description_kind: plain
                        max_items: 1
                      text:
                        nesting_mode: list
                        block:
                          attributes:
                            allow_playback_interruption:
                              type: bool
                              description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                              description_kind: plain
                              computed: true
                            text:
                              type:
                              - list
                              - string
                              description: 'A collection of text response variants. If multiple variants are defined, only one text response variant is returned at runtime. required: true'
                              description_kind: plain
                              optional: true
                          description: 'The text response message. This field is part of a union field ''message'': Only one of ''text'', ''payload'', ''conversationSuccess'', ''outputAudioText'', ''liveAgentHandoff'', ''endInteraction'', ''playAudio'', ''mixedAudio'', ''telephonyTransferCall'', or ''knowledgeInfoCard'' may be set.'
                          description_kind: plain
                        max_items: 1
                    description: The list of rich message responses to present to the user.
                    description_kind: plain
                set_parameter_actions:
                  nesting_mode: list
                  block:
                    attributes:
                      parameter:
                        type: string
                        description: Display name of the parameter.
                        description_kind: plain
                        optional: true
                      value:
                        type: string
                        description: The new JSON-encoded value of the parameter. A null value clears the parameter.
                        description_kind: plain
                        optional: true
                    description: Set parameter values before executing the webhook.
                    description_kind: plain
              description: The fulfillment to be triggered. When the answers from the Knowledge Connector are selected by Dialogflow, you can utitlize the request scoped parameter $request.knowledge.answers (contains up to the 5 highest confidence answers) and $request.knowledge.questions (contains the corresponding questions) to construct the fulfillment.
              description_kind: plain
            max_items: 1
        description: Knowledge connector configuration.
        description_kind: plain
      max_items: 1
    timeouts:
      nesting_mode: single
      block:
        attributes:
          create:
            type: string
            description_kind: plain
            optional: true
          delete:
            type: string
            description_kind: plain
            optional: true
          update:
            type: string
            description_kind: plain
            optional: true
        description_kind: plain
    transition_routes:
      nesting_mode: list
      block:
        attributes:
          condition:
            type: string
            description: The condition to evaluate against form parameters or session parameters. At least one of intent or condition must be specified. When both intent and condition are specified, the transition can only happen when both are fulfilled.
            description_kind: plain
            optional: true
          intent:
            type: string
            description: 'The unique identifier of an Intent. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/intents/<Intent ID>. Indicates that the transition can only happen when the given intent is matched. At least one of intent or condition must be specified. When both intent and condition are specified, the transition can only happen when both are fulfilled.'
            description_kind: plain
            optional: true
          name:
            type: string
            description: The unique identifier of this transition route.
            description_kind: plain
            computed: true
          target_flow:
            type: string
            description: 'The target flow to transition to. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/flows/<Flow ID>.'
            description_kind: plain
            optional: true
          target_page:
            type: string
            description: 'The target page to transition to. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/flows/<Flow ID>/pages/<Page ID>.'
            description_kind: plain
            optional: true
        block_types:
          trigger_fulfillment:
            nesting_mode: list
            block:
              attributes:
                return_partial_responses:
                  type: bool
                  description: 'Whether Dialogflow should return currently queued fulfillment response messages in streaming APIs. If a webhook is specified, it happens before Dialogflow invokes webhook. Warning: 1) This flag only affects streaming API. Responses are still queued and returned once in non-streaming API. 2) The flag can be enabled in any fulfillment but only the first 3 partial responses will be returned. You may only want to apply it to fulfillments that have slow webhooks.'
                  description_kind: plain
                  optional: true
                tag:
                  type: string
                  description: The tag used by the webhook to identify which fulfillment is being called. This field is required if webhook is specified.
                  description_kind: plain
                  optional: true
                webhook:
                  type: string
                  description: 'The webhook to call. Format: projects/<Project ID>/locations/<Location ID>/agents/<Agent ID>/webhooks/<Webhook ID>.'
                  description_kind: plain
                  optional: true
              block_types:
                conditional_cases:
                  nesting_mode: list
                  block:
                    attributes:
                      cases:
                        type: string
                        description: A JSON encoded list of cascading if-else conditions. Cases are mutually exclusive. The first one with a matching condition is selected, all the rest ignored. See [Case](https://cloud.google.com/dialogflow/cx/docs/reference/rest/v3/Fulfillment#case) for the schema.
                        description_kind: plain
                        optional: true
                    description: Conditional cases for this fulfillment.
                    description_kind: plain
                messages:
                  nesting_mode: list
                  block:
                    attributes:
                      channel:
                        type: string
                        description: The channel which the response is associated with. Clients can specify the channel via QueryParameters.channel, and only associated channel response will be returned.
                        description_kind: plain
                        optional: true
                      payload:
                        type: string
                        description: A custom, platform-specific payload.
                        description_kind: plain
                        optional: true
                    block_types:
                      conversation_success:
                        nesting_mode: list
                        block:
                          attributes:
                            metadata:
                              type: string
                              description: Custom metadata. Dialogflow doesn't impose any structure on this.
                              description_kind: plain
                              optional: true
                          description: 'Indicates that the conversation succeeded, i.e., the bot handled the issue that the customer talked to it about. Dialogflow only uses this to determine which conversations should be counted as successful and doesn''t process the metadata in this message in any way. Note that Dialogflow also considers conversations that get to the conversation end page as successful even if they don''t return ConversationSuccess. You may set this, for example: * In the entryFulfillment of a Page if entering the page indicates that the conversation succeeded. * In a webhook response when you determine that you handled the customer issue.'
                          description_kind: plain
                        max_items: 1
                      live_agent_handoff:
                        nesting_mode: list
                        block:
                          attributes:
                            metadata:
                              type: string
                              description: Custom metadata. Dialogflow doesn't impose any structure on this.
                              description_kind: plain
                              optional: true
                          description: 'Indicates that the conversation should be handed off to a live agent. Dialogflow only uses this to determine which conversations were handed off to a human agent for measurement purposes. What else to do with this signal is up to you and your handoff procedures. You may set this, for example: * In the entryFulfillment of a Page if entering the page indicates something went extremely wrong in the conversation. * In a webhook response when you determine that the customer issue can only be handled by a human.'
                          description_kind: plain
                        max_items: 1
                      output_audio_text:
                        nesting_mode: list
                        block:
                          attributes:
                            allow_playback_interruption:
                              type: bool
                              description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                              description_kind: plain
                              computed: true
                            ssml:
                              type: string
                              description: The SSML text to be synthesized. For more information, see SSML.
                              description_kind: plain
                              optional: true
                            text:
                              type: string
                              description: The raw text to be synthesized.
                              description_kind: plain
                              optional: true
                          description: A text or ssml response that is preferentially used for TTS output audio synthesis, as described in the comment on the ResponseMessage message.
                          description_kind: plain
                        max_items: 1
                      play_audio:
                        nesting_mode: list
                        block:
                          attributes:
                            allow_playback_interruption:
                              type: bool
                              description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                              description_kind: plain
                              computed: true
                            audio_uri:
                              type: string
                              description: URI of the audio clip. Dialogflow does not impose any validation on this value. It is specific to the client that reads it.
                              description_kind: plain
                              required: true
                          description: Specifies an audio clip to be played by the client as part of the response.
                          description_kind: plain
                        max_items: 1
                      telephony_transfer_call:
                        nesting_mode: list
                        block:
                          attributes:
                            phone_number:
                              type: string
                              description: Transfer the call to a phone number in E.164 format.
                              description_kind: plain
                              required: true
                          description: Represents the signal that telles the client to transfer the phone call connected to the agent to a third-party endpoint.
                          description_kind: plain
                        max_items: 1
                      text:
                        nesting_mode: list
                        block:
                          attributes:
                            allow_playback_interruption:
                              type: bool
                              description: Whether the playback of this message can be interrupted by the end user's speech and the client can then starts the next Dialogflow request.
                              description_kind: plain
                              computed: true
                            text:
                              type:
                              - list
                              - string
                              description: A collection of text responses.
                              description_kind: plain
                              optional: true
                          description: The text response message.
                          description_kind: plain
                        max_items: 1
                    description: The list of rich message responses to present to the user.
                    description_kind: plain
                set_parameter_actions:
                  nesting_mode: list
                  block:
                    attributes:
                      parameter:
                        type: string
                        description: Display name of the parameter.
                        description_kind: plain
                        optional: true
                      value:
                        type: string
                        description: The new JSON-encoded value of the parameter. A null value clears the parameter.
                        description_kind: plain
                        optional: true
                    description: Set parameter values before executing the webhook.
                    description_kind: plain
              description: The fulfillment to call when the condition is satisfied. At least one of triggerFulfillment and target must be specified. When both are defined, triggerFulfillment is executed first.
              description_kind: plain
            max_items: 1
        description: 'A list of transitions for the transition rules of this page. They route the conversation to another page in the same flow, or another flow. When we are in a certain page, the TransitionRoutes are evalauted in the following order: TransitionRoutes defined in the page with intent specified. TransitionRoutes defined in the transition route groups with intent specified. TransitionRoutes defined in flow with intent specified. TransitionRoutes defined in the transition route groups with intent specified. TransitionRoutes defined in the page with only condition specified. TransitionRoutes defined in the transition route groups with only condition specified.'
        description_kind: plain
  description_kind: plain
