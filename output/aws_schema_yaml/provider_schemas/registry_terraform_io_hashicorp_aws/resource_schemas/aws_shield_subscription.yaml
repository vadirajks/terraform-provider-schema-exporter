version: 0
block:
  attributes:
    auto_renew:
      type: string
      description: Whether to automatically renew the subscription when it expires.
      description_kind: plain
      optional: true
      computed: true
    id:
      type: string
      description_kind: plain
      computed: true
    skip_destroy:
      type: bool
      description_kind: plain
      optional: true
  description_kind: plain
